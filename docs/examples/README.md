# Example Outputs

This directory stores compact, public-safe summaries generated from synthetic
demo seeds.

Use:

```bash
npm run business:example
```

to refresh `business-governance-alpha-summary.json`.

Full generated outputs are intentionally kept under ignored runtime directories
such as `backend/uploads/simulations/<simulation_id>/business/`.
